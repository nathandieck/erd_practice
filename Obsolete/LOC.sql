-- drop table edu
CREATE TABLE loc (
	country VARCHAR,
	latitude DECIMAL,
	longitude DECIMAL,
    code VARCHAR,
    country-code VARCHAR,
    sub-region VARCHAR,
    intermediate-region VARCHAR
);
SELECT * FROM loc